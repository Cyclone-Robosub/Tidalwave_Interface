import QmlProject 1.3

Project {
    mainFile: "main.qml"

    // Add QML files, images, and other resources
    FileSelector {
        path: "."
        files: ["*.qml", "*.png"]
    }

    // Set resource paths
    importPaths: [ "imports" ]
}
